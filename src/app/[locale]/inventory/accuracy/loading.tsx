import { RouteLoadingSkeleton } from '@/components/ui/route-loading-skeleton';

export default function InventoryAccuracyLoading() {
  return <RouteLoadingSkeleton label="กำลังโหลดรายงานความแม่นยำ..." />;
}
