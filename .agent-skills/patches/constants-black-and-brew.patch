diff --git a/src/constants.py b/src/constants.py
index b0b7f9a..a4b3185 100644
--- a/src/constants.py
+++ b/src/constants.py
@@ -12,6 +12,9 @@ from typing import Callable
 from config import PROJECT_ROOT
 
 DATA_DIR = PROJECT_ROOT / "data"
+# Repo root is three levels above vendor AgentSkillOS (…/black-and-brew/.agent-skills/vendor/AgentSkillOS).
+REPO_ROOT = PROJECT_ROOT.parent.parent.parent
+AGENT_SKILLS_ROOT = REPO_ROOT / ".agent-skills"
 
 # ===== Skill Groups Configuration =====
 # Alias mapping for backward compatibility (e.g., "default" -> "skill_seeds")
@@ -51,6 +54,15 @@ SKILL_GROUPS = [
         "tree_path": str(DATA_DIR / "capability_trees" / "tree_skill_10000.yaml"),
         "vector_db_path": str(DATA_DIR / "vector_stores" / "skill_10000"),
     },
+    {
+        "id": "black_and_brew",
+        "name": "BLACKANDBREW ERP Runbooks",
+        "description": "Project-local terminal runbook skills",
+        "skills_dir": str(AGENT_SKILLS_ROOT / "skills"),
+        "tree_path": str(AGENT_SKILLS_ROOT / "data" / "capability_trees" / "tree_black_and_brew.yaml"),
+        "vector_db_path": str(AGENT_SKILLS_ROOT / "data" / "vector_stores" / "black_and_brew"),
+        "is_default": False,
+    },
 ]
 
 
